import React from "react";
import { PageViewProps } from "../AppView";

export default function TopPageView(props: PageViewProps): JSX.Element {
    return (
      <div className="top-page">
        This is Top Page
      </div>
    );
}
